# modelos y cachés
*.pt
__pycache__/
*.pyc
.DS_Store
# logs y temporales
g1_nav.log
missfont.log
ziVNypQO
# rotaciones de goto.log (comprimidas, fuera de git: quedan solo en la máquina que rotó)
archive/goto_hasta_*.log.gz
# (las imágenes de crashes/ se incluyen como dataset; coméntalo si no quieres subirlas)

# entorno virtual local (no debe estar en git)
.venv/

# basura de trabajo que no debe entrar en git
__pycache__/
runs/
"How to use Claude"/
goto.log
dataset/reconstruccion/
